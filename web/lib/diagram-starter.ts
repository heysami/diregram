/**
 * Minimal starter content for a Diagram document.
 *
 * Keep one root line so new files render immediately instead of a blank canvas.
 */
export function makeStarterDiagramMarkdown(): string {
  return 'Root\n';
}
